# PR #39 后变更：生产补查与返工计划

日期：2026-09-21。基线：PR #39 `54ce96ad622c3eb7af2eff37e3c8bc36822980d8`；
审核 HEAD：PR #48 `4ecfc05d535803602861123874ea2ef2bc4f11a8`，tree
`527b9979f8e45b2c38b1cb3e85683f7963304632`。返工分支：`codex/pr39-review-rework`。
本文件记录交付范围、上线顺序与验收条件；测试结果另附本次交付记录。

## 结论与范围

PR #40–48 共 9 个提交、35 个变更文件已经逐项审查，并延伸检查相关合约、
keeper 恢复、数据库、部署和前端消费链。原审核确认 7 项新增/交互问题及 2 项
基线问题；返工时进一步确认 Casino 内存队列在游标落盘后崩溃可遗漏待结算单（B3），
纳入W2的持久恢复。未证明越权提款或资不抵债。本次补充生产只读证据，将旧治理密钥
残留提升为 P1、最先处置的生产风险（本机非root读取路径；未证明已被窃取或远程匿名可读）。完整审查不等于对仓库 1441 个跟踪文件逐行证明，
也不等于生产已经部署了本地修复。

实施顺序是：收紧历史凭据访问 → 本地补丁与反例验证 → 独立复核 → 固定提交的
CI 镜像 → 单链灰度 → 第二链 → 历史凭据退休及治理权限迁移。各阶段分别留证。
不因本地测试通过而宣称线上已修复，也不将 keeper 版本升级混同于 Bank 合约发布。

## 生产事实（只读，2026-09-21 08:00–08:10 UTC）

目标：`root@45.77.243.138`，主机 `vultr`。

| 项目          | 现场证据                                                                                                                              | 影响                                                                        |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| 两个 keeper   | `ghcr.io/arbigamefi/ssot-bbog-keeper:sha-cb26997`，image ID `sha256:41d397507ada29e18b85752e9d100acf919810740f0fe56e1555c02d4491ef57` | 对应 PR #42，不能用工作区 PR #48 推断生产行为                               |
| web           | `ghcr.io/arbigamefi/ssot-bbog-web:sha-4c2820c`，image ID `sha256:3e5baded93baaeb89cd1c7b0eddcbc0e80802985ecda86a697b168965299c276`    | 对应 PR #40，健康检查 healthy                                               |
| 重启          | 两个 keeper 此容器实例 restartCount=0，约运行17小时                                                                                   | 不是跨容器历史连续性证明                                                    |
| 主网 keeper   | `0x440558699040d28975D218caB497338B65960A92`                                                                                          | 当前 signer 已独立                                                          |
| 测试网 keeper | `0xc04d22F83d7494440Dd07ab42BeD01E45E88EC59`                                                                                          | 快照含 bet185 成功结算，latencyMs=3683；单样本，不是延迟分布                |
| 历史密钥      | 24 份 keeper env 备份使用同一旧 key；主机内导出的公开地址为 `0xc8eC9920d573893E888db5D30b2B3B3824B1b684`                              | 与历史治理账户相同；没有输出或下载私钥                                      |
| 链上权限读回  | 主网两家Bank、测试网两家Bank的governance均仍为旧账户；riskInPaused=false；guardian()调用均revert                                      | 旧副本仍对应有效治理权，不能只作为无效历史key处理；不宣称v15 guardian已上线 |
| 权限          | 其中9份主网备份 `0644`；从 `/` 到 env 目录均 `0755`                                                                                   | 普通本机用户可读取这些历史凭据，须优先收紧                                  |
| 体育          | 两条链 `KEEPER_SPORTS_TERMINALIZER_ENABLED=false`、ticket index=false                                                                 | 体育恢复修复是启用前门槛，不能宣称当前线上体育处理中断                      |
| Casino 索引   | 两条链 scanIndex=true，chunk=10，maxChunks=50，RPC间隔250ms                                                                           | 不得快进 settlement cursor                                                  |
| DB            | 只读事务、statement timeout5s、lock timeout2s成功；183条 bets，均84532/finalized                                                      | 索引未显示未结算单，不证明索引历史完整或链上无其他单                        |
| 游标          | 当前主网51594054、测试网47104603，另有旧GameHub游标                                                                                   | 保留按 chain+hub 区分；不能混用或统一重置                                   |
| DB历史表      | 存在 `indexer_cursors_bak_20260919`                                                                                                   | 历史游标操作需与变更记录对账；表名本身不证明丢单                            |
| 日志          | 当前容器24小时可见记录仅5/11行；无 deadlock/error/429关键词                                                                           | 不能外推日志保留期外或未记录错误                                            |
| 磁盘          | 根卷23G，已用20G，空闲2.8G（88%）                                                                                                     | 拉取新镜像前检查空间，禁止在VPS构建及盲目prune                              |

9份0644备份均早于PR #39，属于生产历史遗留；PR #44的脚本新增备份保留问题
继续延长了旧凭据驻留，不能把旧world-readable权限误归因于PR #44。

证据文件保存在本次任务的 `arbigamefi-pr39-rework/evidence/`，仅含公开地址、
模式、计数、镜像和游标等脱敏数据。

## 已执行的生产变更

用户明确批准后，于2026-09-21 08:17 UTC仅执行10项权限收紧：env目录0755→0700、
上述9份主网旧备份0644→0600。随后复核两份live配置和24份备份全部0600；备份数量
仍为24，未删除、未换key、未重启。两个keeper启动时间、signer、restartCount=0均未变，
queueDepth=0，health快照新鲜，web healthy。扫描游标继续前进至主网51594504、测试网47105053。

已关闭普通本机用户经目录/权限读取这些文件的路径；不代表历史密钥从未被复制，也不
代表治理权限或24份副本已退休。脚本补丁和keeper新镜像尚未部署。

## 工作包、验收与依赖

| 包                   | 对应问题            | 本地实施内容                                                                                                             | 必须通过的验收                                                                                                            |
| -------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- |
| W0 凭据应急收口      | R1，生产已确认      | 形成精确文件/权限清单、无密钥值的回滚记录；轮换脚本不再生成旧key备份                                                     | 生产另行执行 chmod；旧备份退休有批准及复查；治理凭据历史泄露风险单独裁决                                                  |
| W1 原子密钥替换      | R1/R2及补充别名反例 | 完整stdin验证、单一有效赋值、拒绝别名/多行/路径链接；临时文件0600、显式flush/fsync/close、原子replace；修正文档回滚      | 真正文件大小限制、fsync/rename故障不截断原文件；后置耐久性失败如实报告；正常替换保留其他字节、owner/mode；无新增旧key副本 |
| W2 Casino 索引完整性 | B2/B3               | 索引写失败传播；完整写入后推进游标；按chain+GameHub从持久事件分页重排待结算单                                            | 写失败不越过缺口；checkpoint后/drain前重启仍结算；分页不被毒性前缀阻塞；正常scan与WSS不回归                               |
| W3 体育恢复          | R3/R4/B1            | 独立 chain+SportsHub 游标；持久市场工作；独立票据历史覆盖与分页；每轮仍受扫描预算约束                                    | 超预算历史跨tick/重启收敛；200+票据全部结算；旧未索引票据恢复；写入失败不提交进度；交易成功后崩溃不重复资金行为           |
| W4 运维快进指令      | R5                  | README、env注释和恢复说明移除直接快进建议                                                                                | 所有样板一致：先完整追赶/核验缺口，不能用索引看起来无pending证明可跳过                                                    |
| W5 会计与暂停验证    | R6                  | production-faithful usedTurnover/HE/ReferralEngine；NAV>=R与多hold交错；真实GameHub暂停后settle/refund；精确turnover上限 | 输局fee=0但PF/XP合法计提；最大赔付/退款保留其他单reserve；guardian越权被拒；上限+1拒绝且高decimals不溢出                  |
| W6 治理操作文档      | R7及相关旧指令      | per-pool Bank调用；EOA/Safe区分；Safe calldata/quorum；用内部调用者归因；准确写出pause对LP/XP/费用的影响                 | 函数签名与当前源码一致；Safe owner EOA不能当治理直接send；每笔readback/receipt清楚                                        |
| W7 发布与回滚        | 发布准备缺口        | 明确 keeper patch、v14线上、v15候选和Safe迁移边界；形成CI镜像与单链灰度步骤                                              | 不以getSSOT ABI检查替代完整版本/字节码/权限核验；不重用DeployV14冒充已完成v15发布                                         |

W1/W5/W6 可并行；W2/W3由同一实施者处理共享runtime/store边界。独立审核者负责
检查反例和回归，不由实施者自行签署最终线上验收。W0权限收紧无需等体育改造。

## 阶段 A：生产凭据收口

1. 锁定只读清单：两份live env与24份历史keeper备份；9份world-readable为最高优先。
   在变更记录中保存文件路径、inode、owner、mode，不能保存密钥内容。
2. 对 env 目录执行 `0700`，对已确认含凭据的live/backup文件执行 `0600`。这是访问
   收紧，不删除凭据，不换signer，不重启容器；完成后逐项stat复核与健康检查。
   回滚通常只需恢复必要访问主体；不应惯例恢复world-readable凭据。
3. 确认新keeper的离线恢复方法、余额和公开地址。新脚本以受审查版本安装，随后
   的轮换不生成旧key备份。无需为了安装脚本再次轮换当前正常keeper。
4. 操作人批准精确旧备份清单后退休24份副本；不将它们打包到本机“备份”目录。
   云快照、旧磁盘、其他导出路径另行调查。unlink不保证物理擦除或历史未被复制。
5. 对旧治理账户做权力清单：每条链、Bank/Hub/Registry/Adapter、pending governance、
   资产/角色/签名者。治理迁移使用独立签署方案；文件清理不能撤销已经复制的私钥。

阶段 A 第2项权限收紧已经用户批准并执行（见上文）；其余生产写操作和密钥退休
仍需分别释放执行范围。本地修复不等待这些决定。

## 阶段 B：本地修复与独立验收

1. 针对上表W1–W6执行反例与正常控制。保留失败故障注入，避免只测实现镜像。
2. Forge PR profile全量unit/diff/invariant；keeper与bet-index专项及全量前端严格测试、
   typecheck/lint、keeper build。工作区已有前端展示改动在首次审核已通过build与19条E2E；
   本次共享包拆分暴露Next对Node ESM的.js导入解析问题，已补充web配置的.ts回退；
   因此重新执行web生产构建、bundle检查、浏览器E2E和Storybook构建。
3. 额外核验真实Postgres迁移/幂等/重启路径；纯内存store测试不能替代SQL行为。
4. 独立复核候选diff的剩余绕过、正常配置与失败生命周期；确认问题后修复并重跑受影响检查。
5. 交付固定diff、测试命令/结果、未覆盖边界和工作区状态。不得把“本地通过”写成生产fixed。

## 阶段 C：keeper 上线

1. 固定审核提交，授权后提交/推送并由现有CI构建linux/amd64不可变镜像；记录完整digest。
   当前运行image ID不是registry manifest digest，回滚前应获取RepoDigests，禁止混用。
2. 检查磁盘余量及新旧镜像大小，只清理已证明无用的构建/镜像对象；保留当前运行回滚镜像
   和Postgres数据卷。不得在VPS构建，不执行 `docker system prune --volumes`。
3. 备份数据库由运维批准的离线机制承担，保留当前cursor值与配置（去除密钥）。新增恢复表
   必须是向后兼容追加；回滚旧keeper不删除表或重置cursor。
4. 先Sepolia keeper：仅替换镜像，sports保持false，当前dedicated signer不变；健康、队列、
   scan cursor连续、DB错误及RPC额度达标后再主网。两次启动至少间隔20秒并确认第一条链
   migration已结束；若发现启动deadlock则停止扩展，定位迁移串行化问题。
5. 出现启动失败、进度越过未处理区间、连续索引错误或异常交易时，停止后续发布，恢复上一
   已知digest及原配置。不能用fast-forward cursor让告警消失。恢复后核对链上事件与DB缺口。
6. 至少观察完整工作周期；测试网需有真实结算receipt证明新镜像正常。现有bet185历史交易
   不证明新镜像；无新交易只能报告启动/扫描通过。真实下注或资金操作单独授权。

## 阶段 D：体育启用与 v15/Safe 发布

体育新恢复路径需要可用Postgres；启用前配置校验明确拒绝无DB模式，Casino无DB模式
保持支持。这是有意的兼容性变更，应写入发布说明。票据扫描起点必须不晚于SportsHub部署，
market历史及票据历史覆盖结束前不能宣称市场处理完成；先在测试网演练历史超过budget、
200+票据、数据库故障、进程重启，再考虑主网启用。当前生产继续保持false。

Bank v15与Safe是独立合约发布事项。当前DeployV14要求deployer==gov，不能简单传入Safe
地址完成新治理部署。上线前必须具备版本专属部署/产物路径、完整ABI与字节码核验、
Safe链ID/owners/threshold/recovery方案、所有合约权限读回、guardian配置及真实Safe暂停/
撤销/恢复演练。现有v14地址/ABI和历史release lock保留为历史事实，不覆盖伪装成v15。
若使用现有合约两步治理转移，必须单独列出每个target的提议/接受交易及失败恢复。

## 完成定义

- 本地完成：W1–W6代码/文档和必要验证通过、独立复核无未处理的确认问题。
- 生产keeper完成：固定digest双链发布、连续扫描与真实测试网settlement证据齐全。
- 凭据风险完成：权限收口、旧副本退休及旧治理权限处置均有执行和复查证据。
- v15/Safe完成：独立发布门槛全部兑现。当前计划或源码存在均不构成上线验收。
