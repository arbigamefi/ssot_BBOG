# Sepolia v1.5 业务验收安排

链 ID 固定为 84532，发布摘要为 `0xae662a55eef716a97f1a2d2bfad389507b86d0aff72b32a162b90fcb59920c6b`。7 个合约的 Safe 治理交接已执行并核验。USDC Bank 已由 Safe 解除暂停，WETH Bank 保持暂停。独立内部 Web/keeper/PostgreSQL 使用新 Sepolia 合约，公开 Web 和主网仍使用旧合约。

## 已完成

- 发布签名、严格制品校验、治理校验、打包及 `ssot:sync` 导入。
- 新合约的实时只读 smoke 检查与 19 项权限边界模拟。
- 应用类型检查及 778 项测试；另用全新本机 PostgreSQL 运行此前跳过的 6 项恢复集成测试，全部通过。
- 创建独立本机验收数据库 `arbigamefi_v15`，绑定 `127.0.0.1:55432`，使用新卷，不导入旧 bet_id 投影。数据库恢复测试使用独立临时 schema，不写生产数据。

## 当前进度（2026-09-22）

步骤 1—6 已推进到八游戏真实投注全部结算并完成链上、Bank 和 PostgreSQL 对账。源码验证已补齐，17 个地址中 5 个 Exact Match、12 个 Similar Match、0 个未验证。40 USDC LP 已存入；八笔 stake 合计 0.572652 USDC，净赔付 0.417540 USDC，钱包剩余 9.844888 USDC。空队列 keeper 重启检查通过；仍需处理中重启、持久写入失败、真实业务退款、guardian/Safe 演练和 LP/费用处置。浏览器收据只读核对通过，进入游戏时的链连续性与钱包交互仍待验收。详见 [实际交易及边界](base-sepolia-usdc-canary.json)。

## 执行顺序

1. 完成当前 PR 的构建和 CI。使用签名发布包中的真实新地址，不复制旧配置中的起始区块。生产双链 v1.5 镜像门禁仍保持开启。
2. Owner 已选择提供测试 USDC；验收钱包为 `0x93ac87413E17d01CBa37B6317f64890bF7f99aC3`，Owner 已将 50 测试 USDC 转入该部署地址并授权直接使用；用已核验的部署 keystore 签名，最多 40 USDC 入池、最多 10 USDC 用于投注；核验链 ID、USDC 地址与余额、gas 余额及 LP/投注预算。现有部署账户的测试 ETH 继续用于 gas，不包装为 WETH。
3. 启动只面向本机/内部网络的新 Web 与数据库。确认页面和 API 使用新 Sepolia GameHub、USDC Bank，数据为空。此阶段维持新 Bank 暂停。
4. 交接 Sepolia keeper 的唯一签名使用权：旧 keeper 完成队列检查并停止后，新 keeper 才可使用同一地址。起点为本次签名发布的区块，使用全新健康文件和数据库；不同时运行两个同地址发送进程。主网 keeper 与旧 Caddy 此阶段继续服务。
5. 在运行条件和预算全部具备后，生成并模拟仅针对新 USDC Bank `0xEa3845f08a273257c3d3458D26e7bd4234330Dd4` 的 Safe 解除暂停包，再由真实 2/3 owners 执行。WETH Bank 继续暂停。
6. 先模拟，再由验收钱包执行有限额度 USDC 授权、LP 存入、小额投注。逐笔保存交易/回执、BetPlaced、VRF 回调、终态、Bank 账本、数据库投影与 UI 收据，核对本金/赔付/费用，禁止只凭 keeper 日志判定成功。
7. 执行 keeper 重启与索引写入失败恢复演练；按真实可实现路径验收退款，保留失败证据，不把本地 fork 模拟当成真实链上退款。guardian 暂停后核验新风险拒绝、已有债务出口可用；恢复须由 Safe 执行。记录实际预算消耗和剩余资金。
8. 验收完成后归还或明确保留测试 LP/费用义务，并将 Bank 恢复到约定暂停状态。再进入 Base 主网部署、双链镜像发布和生产切换。

## 退出条件

完整业务验收必须有真实签名与链上事件、全新数据库/UI 一致性及重启/故障恢复证据。目前尚未满足；通过的单元测试、只读模拟、空池 smoke 和治理接收不能替代它。测试网通过前不推进主网广播，不删除旧卷、密钥和未结清资产记录。

历史阶段：本机 Web 预验收已完成：新 Sepolia release 与独立 PostgreSQL 均为 `ok`，数据库行数为 0；keeper 尚未交接，整体健康状态如实为 `degraded`。预验收进程已停止，保留验收数据库；这不是常驻生产实例；此历史记录已由上述 VPS 独立内部栈推进，不能作为当前 keeper 状态。
