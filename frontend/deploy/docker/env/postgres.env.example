# Copy to deploy/docker/env/postgres.env.
# This file is consumed by Docker Compose and must not be committed with real values.

POSTGRES_DB=arbigamefi
POSTGRES_USER=arbigamefi
POSTGRES_PASSWORD=REPLACE_WITH_LONG_RANDOM_PASSWORD
