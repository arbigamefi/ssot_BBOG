# Copy to deploy/docker/env/proxy.env.

ARBGAMEFI_DOMAIN=arbigamefi.com
ACME_EMAIL=ops@arbigamefi.com
# Caddy obtains and renews server certificates through HTTP-01 on port 80.
# The canonical domain and its www/dapp aliases must all resolve to this host.
