# Page Spec — Ops (Read-only)

## Route
- `/ops`

## Purpose
Provide minimal operational observability in the UI without creating an admin console.

## Modules
- `IndexerStatusCard` (latest synced block, lag, confirmations)
- `HubBalanceCard` (native balance if exposed)
- `VRFBacklogCard` (if inferable)

## Notes
- This page is read-only and MUST not include privileged actions.
